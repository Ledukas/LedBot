# Secrets / credentials
.env
service_account.json

# Runtime data (real member/game data — not source)
*.db
winners.json

# Local backups (generated by scripts/backup_db.py)
Backups/*
!Backups/.gitkeep

# Python
__pycache__/
*.pyc
venv/

# Local tool state (DB Browser for SQLite session; machine-specific paths)
*.sqbpro
